<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <main class="centered">
                <div class="coursecard__container">
                <article class="coursecard coursecard__item" >
                    <div class="coursecard__image">
                        <img src="<%= course.image %>" alt="<%= course.title %>">
                    </div>
                    <div class="coursecard__content">
                        <p class="course__title"><%= course.title %></p>
                    </div>
                </article>
                <h3 class="coursecard__description"><%= course.description %></h3>
                <h3 class="coursecard__content"><%= course.content %></h3>
    
                </div>
            </main>
            <%- include('../includes/end.ejs') %>